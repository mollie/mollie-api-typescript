/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { delayedRoutingList } from "../../funcs/delayedRoutingList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PaymentListRoutesRequest$inboundSchema,
};

export const tool$delayedRoutingList: ToolDefinition<typeof args> = {
  name: "delayed-routing-list",
  description: `List payment routes

Retrieve a list of all routes created for a specific payment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await delayedRoutingList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
