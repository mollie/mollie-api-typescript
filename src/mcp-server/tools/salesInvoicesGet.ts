/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { salesInvoicesGet } from "../../funcs/salesInvoicesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSalesInvoiceRequest$inboundSchema,
};

export const tool$salesInvoicesGet: ToolDefinition<typeof args> = {
  name: "sales-invoices-get",
  description: `Get sales invoice

> 🚧 Beta feature
>
> This feature is currently in beta testing, and the final specification may still change.

Retrieve a single sales invoice by its ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await salesInvoicesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
