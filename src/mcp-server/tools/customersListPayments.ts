/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersListPayments } from "../../funcs/customersListPayments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomerPaymentsRequest$inboundSchema,
};

export const tool$customersListPayments: ToolDefinition<typeof args> = {
  name: "customers-list-payments",
  description: `List customer payments

Retrieve all payments linked to the customer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersListPayments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
