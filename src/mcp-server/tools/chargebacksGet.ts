/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { chargebacksGet } from "../../funcs/chargebacksGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetChargebackRequest$inboundSchema,
};

export const tool$chargebacksGet: ToolDefinition<typeof args> = {
  name: "chargebacks-get",
  description: `Get payment chargeback

Retrieve a single payment chargeback by its ID and the ID of its parent payment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await chargebacksGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
