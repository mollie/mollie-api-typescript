/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookEventsGet } from "../../funcs/webhookEventsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWebhookEventRequest$inboundSchema,
};

export const tool$webhookEventsGet: ToolDefinition<typeof args> = {
  name: "webhook-events-get",
  description: `Get a Webhook Event

Retrieve a single webhook event object by its event ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhookEventsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
