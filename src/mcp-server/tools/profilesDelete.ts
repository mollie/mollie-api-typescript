/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { profilesDelete } from "../../funcs/profilesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteProfileRequest$inboundSchema,
};

export const tool$profilesDelete: ToolDefinition<typeof args> = {
  name: "profiles-delete",
  description: `Delete profile

Delete a profile. A deleted profile and its related credentials can no longer be used for accepting payments.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await profilesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
