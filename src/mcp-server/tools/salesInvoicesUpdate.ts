/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { salesInvoicesUpdate } from "../../funcs/salesInvoicesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSalesInvoiceRequest$inboundSchema,
};

export const tool$salesInvoicesUpdate: ToolDefinition<typeof args> = {
  name: "sales-invoices-update",
  description: `Update sales invoice

> 🚧 Beta feature
>
> This feature is currently in beta testing, and the final specification may still change.

Certain details of an existing sales invoice can be updated. For \`draft\` it is all values listed below, but for
statuses \`paid\` and \`issued\` there are certain additional requirements (\`paymentDetails\` and \`emailDetails\`,
respectively).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await salesInvoicesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
