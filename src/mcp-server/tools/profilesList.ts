/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { profilesList } from "../../funcs/profilesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListProfilesRequest$inboundSchema.optional(),
};

export const tool$profilesList: ToolDefinition<typeof args> = {
  name: "profiles-list",
  description: `List profiles

Retrieve a list of all of your profiles.

The results are paginated.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await profilesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
