/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsUpdate } from "../../funcs/paymentsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdatePaymentRequest$inboundSchema,
};

export const tool$paymentsUpdate: ToolDefinition<typeof args> = {
  name: "payments-update",
  description: `Update payment

Certain details of an existing payment can be updated.

Updating the payment details will not result in a webhook call.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
