/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsGet } from "../../funcs/permissionsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPermissionRequest$inboundSchema,
};

export const tool$permissionsGet: ToolDefinition<typeof args> = {
  name: "permissions-get",
  description: `Get permission

Retrieve a single permission by its ID, and see if the permission is granted to the current access token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await permissionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
