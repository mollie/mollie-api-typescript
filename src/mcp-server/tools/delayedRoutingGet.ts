/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { delayedRoutingGet } from "../../funcs/delayedRoutingGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PaymentGetRouteRequest$inboundSchema,
};

export const tool$delayedRoutingGet: ToolDefinition<typeof args> = {
  name: "delayed-routing-get",
  description: `Get a delayed route

Retrieve a single route created for a specific payment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await delayedRoutingGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
