/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { delayedRoutingCreate } from "../../funcs/delayedRoutingCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PaymentCreateRouteRequest$inboundSchema,
};

export const tool$delayedRoutingCreate: ToolDefinition<typeof args> = {
  name: "delayed-routing-create",
  description: `Create a delayed route

Create a route for a specific payment.
The routed amount is credited to the account of your customer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await delayedRoutingCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
