/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { capturesGet } from "../../funcs/capturesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCaptureRequest$inboundSchema,
};

export const tool$capturesGet: ToolDefinition<typeof args> = {
  name: "captures-get",
  description: `Get capture

Retrieve a single payment capture by its ID and the ID of its parent
payment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await capturesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
