/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { terminalsGet } from "../../funcs/terminalsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTerminalRequest$inboundSchema,
};

export const tool$terminalsGet: ToolDefinition<typeof args> = {
  name: "terminals-get",
  description: `Get terminal

Retrieve a single terminal by its ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await terminalsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
