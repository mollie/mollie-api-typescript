/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { salesInvoicesDelete } from "../../funcs/salesInvoicesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSalesInvoiceRequest$inboundSchema,
};

export const tool$salesInvoicesDelete: ToolDefinition<typeof args> = {
  name: "sales-invoices-delete",
  description: `Delete sales invoice

> 🚧 Beta feature
>
> This feature is currently in beta testing, and the final specification may still change.

Sales invoices which are in status \`draft\` can be deleted. For all other statuses, please use the
[Update sales invoice](update-sales-invoice) endpoint instead.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await salesInvoicesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
