/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ebe4cd5cb357
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Mode, Mode$inboundSchema } from "./mode.js";
import {
  ProfileReviewStatusResponse,
  ProfileReviewStatusResponse$inboundSchema,
} from "./profilereviewstatusresponse.js";
import { Url, Url$inboundSchema } from "./url.js";

/**
 * The profile status determines whether the profile is able to receive live payments.
 *
 * @remarks
 *
 * * `unverified`: The profile has not been verified yet and can only be used to create test payments.
 * * `verified`: The profile has been verified and can be used to create live payments and test payments.
 * * `blocked`: The profile is blocked and can no longer be used or changed.
 */
export const ListProfileResponseStatus = {
  Unverified: "unverified",
  Verified: "verified",
  Blocked: "blocked",
} as const;
/**
 * The profile status determines whether the profile is able to receive live payments.
 *
 * @remarks
 *
 * * `unverified`: The profile has not been verified yet and can only be used to create test payments.
 * * `verified`: The profile has been verified and can be used to create live payments and test payments.
 * * `blocked`: The profile is blocked and can no longer be used or changed.
 */
export type ListProfileResponseStatus = OpenEnum<
  typeof ListProfileResponseStatus
>;

/**
 * Present if changes have been made that have not yet been approved by Mollie. Changes to test profiles are approved
 *
 * @remarks
 * automatically, unless a switch to a live profile has been requested. The review object will therefore usually be
 * `null` in test mode.
 */
export type ListProfileResponseReview = {
  /**
   * The status of the requested changes.
   */
  status?: ProfileReviewStatusResponse | undefined;
};

/**
 * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
 */
export type ListProfileResponseLinks = {
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  self?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  dashboard?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  chargebacks?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  methods?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  payments?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  refunds?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  checkoutPreviewUrl?: Url | undefined;
};

export type ListProfileResponse = {
  /**
   * Indicates the response contains a profile object. Will always contain the string `profile` for this endpoint.
   */
  resource: string;
  /**
   * The identifier uniquely referring to this profile. Example: `pfl_v9hTwCvYqw`.
   */
  id: string;
  /**
   * Whether this entity was created in live mode or in test mode.
   */
  mode: Mode;
  /**
   * The profile's name, this will usually reflect the trade name or brand name of the profile's website or
   *
   * @remarks
   * application.
   */
  name: string;
  /**
   * The URL to the profile's website or application. Only `https` or `http` URLs are allowed. No `@` signs are
   *
   * @remarks
   * allowed.
   */
  website: string;
  /**
   * The email address associated with the profile's trade name or brand.
   *
   * @remarks
   *
   * If the domain contains non-ASCII characters, encode it as Punycode per [RFC 3492](https://www.rfc-editor.org/rfc/rfc3492).
   */
  email: string;
  /**
   * The phone number associated with the profile's trade name or brand.
   */
  phone: string;
  /**
   * The products or services offered by the profile's website or application.
   */
  description?: string | undefined;
  /**
   * A list of countries where you expect that the majority of the profile's customers reside,
   *
   * @remarks
   * in [ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) format.
   */
  countriesOfActivity?: Array<string> | undefined;
  /**
   * The industry associated with the profile's trade name or brand. Please refer to the
   *
   * @remarks
   * [business category list](common-data-types#business-category) for all possible options.
   */
  businessCategory: string;
  status: ListProfileResponseStatus;
  /**
   * Present if changes have been made that have not yet been approved by Mollie. Changes to test profiles are approved
   *
   * @remarks
   * automatically, unless a switch to a live profile has been requested. The review object will therefore usually be
   * `null` in test mode.
   */
  review?: ListProfileResponseReview | undefined;
  /**
   * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
   */
  createdAt: string;
  /**
   * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
   */
  links: ListProfileResponseLinks;
};

/** @internal */
export const ListProfileResponseStatus$inboundSchema: z.ZodType<
  ListProfileResponseStatus,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(ListProfileResponseStatus);

/** @internal */
export const ListProfileResponseReview$inboundSchema: z.ZodType<
  ListProfileResponseReview,
  z.ZodTypeDef,
  unknown
> = z.object({
  status: ProfileReviewStatusResponse$inboundSchema.optional(),
});

export function listProfileResponseReviewFromJSON(
  jsonString: string,
): SafeParseResult<ListProfileResponseReview, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListProfileResponseReview$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListProfileResponseReview' from JSON`,
  );
}

/** @internal */
export const ListProfileResponseLinks$inboundSchema: z.ZodType<
  ListProfileResponseLinks,
  z.ZodTypeDef,
  unknown
> = z.object({
  self: Url$inboundSchema.optional(),
  dashboard: Url$inboundSchema.optional(),
  chargebacks: Url$inboundSchema.optional(),
  methods: Url$inboundSchema.optional(),
  payments: Url$inboundSchema.optional(),
  refunds: Url$inboundSchema.optional(),
  checkoutPreviewUrl: Url$inboundSchema.optional(),
});

export function listProfileResponseLinksFromJSON(
  jsonString: string,
): SafeParseResult<ListProfileResponseLinks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListProfileResponseLinks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListProfileResponseLinks' from JSON`,
  );
}

/** @internal */
export const ListProfileResponse$inboundSchema: z.ZodType<
  ListProfileResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  resource: z.string(),
  id: z.string(),
  mode: Mode$inboundSchema,
  name: z.string(),
  website: z.string(),
  email: z.string(),
  phone: z.string(),
  description: z.string().optional(),
  countriesOfActivity: z.array(z.string()).optional(),
  businessCategory: z.string(),
  status: ListProfileResponseStatus$inboundSchema,
  review: z.lazy(() => ListProfileResponseReview$inboundSchema).optional(),
  createdAt: z.string(),
  _links: z.lazy(() => ListProfileResponseLinks$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
  });
});

export function listProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListProfileResponse' from JSON`,
  );
}
