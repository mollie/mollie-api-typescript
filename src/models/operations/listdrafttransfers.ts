/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: de1a951c2027
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type ListDraftTransfersGlobals = {
  /**
   * Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
   *
   * @remarks
   * parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
   * setting the `testmode` query parameter to `true`.
   *
   * Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
   */
  testmode?: boolean | undefined;
};

export type ListDraftTransfersRequest = {
  /**
   * Provide an ID to start the result set from the item with the given ID and onwards. This allows you to paginate the
   *
   * @remarks
   * result set.
   */
  from?: string | null | undefined;
  /**
   * The maximum number of items to return. Defaults to 50 items.
   */
  limit?: number | null | undefined;
  /**
   * Filter the list by draft transfer status. Omit to return draft transfers in any status.
   */
  status?: models.DraftTransferStatus | undefined;
  /**
   * Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
   *
   * @remarks
   * parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
   * setting the `testmode` query parameter to `true`.
   *
   * Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
   */
  testmode?: boolean | undefined;
  /**
   * A unique key to ensure idempotent requests. This key should be a UUID v4 string.
   */
  idempotencyKey?: string | undefined;
};

export type ListDraftTransfersEmbedded = {
  /**
   * An array of draft transfer objects.
   */
  draftTransfers: Array<models.ListDraftTransferResponse>;
};

/**
 * A list of draft transfer objects.
 */
export type ListDraftTransfersResponseBody = {
  /**
   * The number of items in this result set. If more items are available, a `_links.next` URL will be present in the result
   *
   * @remarks
   * as well.
   *
   * The maximum number of items per result set is controlled by the `limit` property provided in the request. The default
   * limit is 50 items.
   */
  count: number;
  embedded: ListDraftTransfersEmbedded;
  /**
   * Links to help navigate through the lists of items. Every URL object will contain an `href` and a `type` field.
   */
  links: models.ListLinks;
};

export type ListDraftTransfersResponse = {
  result: ListDraftTransfersResponseBody;
};

/** @internal */
export type ListDraftTransfersRequest$Outbound = {
  from?: string | null | undefined;
  limit?: number | null | undefined;
  status?: string | undefined;
  testmode?: boolean | undefined;
  "idempotency-key"?: string | undefined;
};

/** @internal */
export const ListDraftTransfersRequest$outboundSchema: z.ZodType<
  ListDraftTransfersRequest$Outbound,
  z.ZodTypeDef,
  ListDraftTransfersRequest
> = z.object({
  from: z.nullable(z.string()).optional(),
  limit: z.nullable(z.number().int()).optional(),
  status: models.DraftTransferStatus$outboundSchema.optional(),
  testmode: z.boolean().optional(),
  idempotencyKey: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    idempotencyKey: "idempotency-key",
  });
});

export function listDraftTransfersRequestToJSON(
  listDraftTransfersRequest: ListDraftTransfersRequest,
): string {
  return JSON.stringify(
    ListDraftTransfersRequest$outboundSchema.parse(listDraftTransfersRequest),
  );
}

/** @internal */
export const ListDraftTransfersEmbedded$inboundSchema: z.ZodType<
  ListDraftTransfersEmbedded,
  z.ZodTypeDef,
  unknown
> = z.object({
  draft_transfers: z.array(models.ListDraftTransferResponse$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "draft_transfers": "draftTransfers",
  });
});

export function listDraftTransfersEmbeddedFromJSON(
  jsonString: string,
): SafeParseResult<ListDraftTransfersEmbedded, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListDraftTransfersEmbedded$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListDraftTransfersEmbedded' from JSON`,
  );
}

/** @internal */
export const ListDraftTransfersResponseBody$inboundSchema: z.ZodType<
  ListDraftTransfersResponseBody,
  z.ZodTypeDef,
  unknown
> = z.object({
  count: z.number().int(),
  _embedded: z.lazy(() => ListDraftTransfersEmbedded$inboundSchema),
  _links: models.ListLinks$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "_embedded": "embedded",
    "_links": "links",
  });
});

export function listDraftTransfersResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<ListDraftTransfersResponseBody, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListDraftTransfersResponseBody$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListDraftTransfersResponseBody' from JSON`,
  );
}

/** @internal */
export const ListDraftTransfersResponse$inboundSchema: z.ZodType<
  ListDraftTransfersResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: z.lazy(() => ListDraftTransfersResponseBody$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listDraftTransfersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListDraftTransfersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListDraftTransfersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListDraftTransfersResponse' from JSON`,
  );
}
