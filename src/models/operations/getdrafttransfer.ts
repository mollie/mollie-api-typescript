/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 51ab3e7f957e
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";

export type GetDraftTransferGlobals = {
  /**
   * Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
   *
   * @remarks
   * parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
   * setting the `testmode` query parameter to `true`.
   *
   * Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
   */
  testmode?: boolean | undefined;
};

export type GetDraftTransferRequest = {
  /**
   * Provide the ID of the related draft transfer.
   */
  draftTransferId: string;
  /**
   * Most API credentials are specifically created for either live mode or test mode. In those cases the `testmode` query
   *
   * @remarks
   * parameter must not be sent. For organization-level credentials such as OAuth access tokens, you can enable test mode by
   * setting the `testmode` query parameter to `true`.
   *
   * Test entities cannot be retrieved when the endpoint is set to live mode, and vice versa.
   */
  testmode?: boolean | undefined;
  /**
   * A unique key to ensure idempotent requests. This key should be a UUID v4 string.
   */
  idempotencyKey?: string | undefined;
};

/** @internal */
export type GetDraftTransferRequest$Outbound = {
  draftTransferId: string;
  testmode?: boolean | undefined;
  "idempotency-key"?: string | undefined;
};

/** @internal */
export const GetDraftTransferRequest$outboundSchema: z.ZodType<
  GetDraftTransferRequest$Outbound,
  z.ZodTypeDef,
  GetDraftTransferRequest
> = z.object({
  draftTransferId: z.string(),
  testmode: z.boolean().optional(),
  idempotencyKey: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    idempotencyKey: "idempotency-key",
  });
});

export function getDraftTransferRequestToJSON(
  getDraftTransferRequest: GetDraftTransferRequest,
): string {
  return JSON.stringify(
    GetDraftTransferRequest$outboundSchema.parse(getDraftTransferRequest),
  );
}
