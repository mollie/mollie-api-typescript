/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d5e2632adf8f
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export const OauthGenerateTokensServerList = [
  "https://api.mollie.com/oauth2",
] as const;

export type OauthGenerateTokensRequestBody = {
  grantType: models.OauthGrantType;
  /**
   * The authorization code you received when creating the authorization. Only use this field when using
   *
   * @remarks
   * grant type `authorization_code`.
   */
  code?: string | undefined;
  /**
   * The refresh token you received when creating the authorization. Only use this field when using grant
   *
   * @remarks
   * type `refresh_token`.
   */
  refreshToken?: string | undefined;
  /**
   * The URL the merchant is sent back to once the request has been authorized. It must match the URL you set
   *
   * @remarks
   * when registering your app.
   *
   * For consecutive refresh token requests, this parameter is required only if the initial authorization
   * code grant request also contained a `redirect_uri`.
   */
  redirectUri?: string | undefined;
};

export type OauthGenerateTokensRequest = {
  /**
   * A unique key to ensure idempotent requests. This key should be a UUID v4 string.
   */
  idempotencyKey?: string | undefined;
  requestBody?: OauthGenerateTokensRequestBody | undefined;
};

/**
 * The newly generated access token and refresh token.
 */
export type OauthGenerateTokensResponse = {
  /**
   * The app access token, with which you will be able to access the Mollie API on the merchant's behalf.
   */
  accessToken?: string | undefined;
  /**
   * The refresh token, with which you will be able to retrieve new app access tokens on this endpoint. The
   *
   * @remarks
   * refresh token does not expire.
   */
  refreshToken?: string | undefined;
  /**
   * The number of seconds left before the app access token expires. Be sure to renew your app access token
   *
   * @remarks
   * before this reaches zero.
   */
  expiresIn?: number | undefined;
  /**
   * As per OAuth standards, the provided app access token can only be used with `bearer` authentication.
   *
   * @remarks
   *
   * Possible values: `bearer`
   */
  tokenType?: string | undefined;
  /**
   * A space-separated list of [permissions](https://docs.mollie.com/docs/permissions).
   */
  scope?: string | undefined;
};

/** @internal */
export type OauthGenerateTokensRequestBody$Outbound = {
  grant_type: string;
  code?: string | undefined;
  refresh_token?: string | undefined;
  redirect_uri?: string | undefined;
};

/** @internal */
export const OauthGenerateTokensRequestBody$outboundSchema: z.ZodType<
  OauthGenerateTokensRequestBody$Outbound,
  z.ZodTypeDef,
  OauthGenerateTokensRequestBody
> = z.object({
  grantType: models.OauthGrantType$outboundSchema,
  code: z.string().optional(),
  refreshToken: z.string().optional(),
  redirectUri: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    grantType: "grant_type",
    refreshToken: "refresh_token",
    redirectUri: "redirect_uri",
  });
});

export function oauthGenerateTokensRequestBodyToJSON(
  oauthGenerateTokensRequestBody: OauthGenerateTokensRequestBody,
): string {
  return JSON.stringify(
    OauthGenerateTokensRequestBody$outboundSchema.parse(
      oauthGenerateTokensRequestBody,
    ),
  );
}

/** @internal */
export type OauthGenerateTokensRequest$Outbound = {
  "idempotency-key"?: string | undefined;
  RequestBody?: OauthGenerateTokensRequestBody$Outbound | undefined;
};

/** @internal */
export const OauthGenerateTokensRequest$outboundSchema: z.ZodType<
  OauthGenerateTokensRequest$Outbound,
  z.ZodTypeDef,
  OauthGenerateTokensRequest
> = z.object({
  idempotencyKey: z.string().optional(),
  requestBody: z.lazy(() => OauthGenerateTokensRequestBody$outboundSchema)
    .optional(),
}).transform((v) => {
  return remap$(v, {
    idempotencyKey: "idempotency-key",
    requestBody: "RequestBody",
  });
});

export function oauthGenerateTokensRequestToJSON(
  oauthGenerateTokensRequest: OauthGenerateTokensRequest,
): string {
  return JSON.stringify(
    OauthGenerateTokensRequest$outboundSchema.parse(oauthGenerateTokensRequest),
  );
}

/** @internal */
export const OauthGenerateTokensResponse$inboundSchema: z.ZodType<
  OauthGenerateTokensResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  access_token: z.string().optional(),
  refresh_token: z.string().optional(),
  expires_in: z.number().int().optional(),
  token_type: z.string().optional(),
  scope: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "access_token": "accessToken",
    "refresh_token": "refreshToken",
    "expires_in": "expiresIn",
    "token_type": "tokenType",
  });
});

export function oauthGenerateTokensResponseFromJSON(
  jsonString: string,
): SafeParseResult<OauthGenerateTokensResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OauthGenerateTokensResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OauthGenerateTokensResponse' from JSON`,
  );
}
