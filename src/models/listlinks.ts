/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1051faf59980
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Url, Url$inboundSchema } from "./url.js";
import { UrlNullable, UrlNullable$inboundSchema } from "./urlnullable.js";

/**
 * Links to help navigate through the lists of items. Every URL object will contain an `href` and a `type` field.
 */
export type ListLinks = {
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  self: Url;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  previous: UrlNullable | null;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  next: UrlNullable | null;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  documentation: Url;
};

/** @internal */
export const ListLinks$inboundSchema: z.ZodType<
  ListLinks,
  z.ZodTypeDef,
  unknown
> = z.object({
  self: Url$inboundSchema,
  previous: z.nullable(UrlNullable$inboundSchema),
  next: z.nullable(UrlNullable$inboundSchema),
  documentation: Url$inboundSchema,
});

export function listLinksFromJSON(
  jsonString: string,
): SafeParseResult<ListLinks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListLinks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListLinks' from JSON`,
  );
}
