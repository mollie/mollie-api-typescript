/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 3cd06a59ee6b
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Url, Url$inboundSchema } from "./url.js";

/**
 * URLs of images representing the payment method issuer.
 */
export type VoucherImage = {
  /**
   * The URL pointing to an icon of 32 by 24 pixels.
   */
  size1x?: string | undefined;
  /**
   * The URL pointing to an icon of 64 by 48 pixels.
   */
  size2x?: string | undefined;
  /**
   * The URL pointing to a vector version of the icon. Usage of this format is preferred, since the icon can
   *
   * @remarks
   * scale to any desired size without compromising visual quality.
   */
  svg?: string | undefined;
};

/**
 * The status of the issuer.
 *
 * @remarks
 * If the status is `pending-issuer`, an additional action from your side may be required with the issuer.
 */
export const VoucherStatus = {
  Activated: "activated",
  PendingIssuer: "pending-issuer",
} as const;
/**
 * The status of the issuer.
 *
 * @remarks
 * If the status is `pending-issuer`, an additional action from your side may be required with the issuer.
 */
export type VoucherStatus = OpenEnum<typeof VoucherStatus>;

/**
 * Information regarding the *contractor*. Only relevant for `voucher` issuers.
 */
export type Contractor = {
  id?: string | undefined;
  name?: string | undefined;
  contractId?: string | undefined;
};

/**
 * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
 */
export type VoucherLinks = {
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  self?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  documentation?: Url | undefined;
};

export type Voucher = {
  /**
   * Indicates the response contains a payment method issuer object. Will always contain the string `issuer` for this
   *
   * @remarks
   * endpoint.
   */
  resource: string;
  /**
   * The unique identifier of the payment method issuer.
   */
  id: string;
  /**
   * The full name of the payment method issuer.
   */
  name: string;
  /**
   * URLs of images representing the payment method issuer.
   */
  image: VoucherImage;
  status: VoucherStatus;
  /**
   * Information regarding the *contractor*. Only relevant for `voucher` issuers.
   */
  contractor: Contractor;
  /**
   * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
   */
  links: VoucherLinks;
};

/** @internal */
export const VoucherImage$inboundSchema: z.ZodType<
  VoucherImage,
  z.ZodTypeDef,
  unknown
> = z.object({
  size1x: z.string().optional(),
  size2x: z.string().optional(),
  svg: z.string().optional(),
});

export function voucherImageFromJSON(
  jsonString: string,
): SafeParseResult<VoucherImage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VoucherImage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VoucherImage' from JSON`,
  );
}

/** @internal */
export const VoucherStatus$inboundSchema: z.ZodType<
  VoucherStatus,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(VoucherStatus);

/** @internal */
export const Contractor$inboundSchema: z.ZodType<
  Contractor,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  name: z.string().optional(),
  contractId: z.string().optional(),
});

export function contractorFromJSON(
  jsonString: string,
): SafeParseResult<Contractor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Contractor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Contractor' from JSON`,
  );
}

/** @internal */
export const VoucherLinks$inboundSchema: z.ZodType<
  VoucherLinks,
  z.ZodTypeDef,
  unknown
> = z.object({
  self: Url$inboundSchema.optional(),
  documentation: Url$inboundSchema.optional(),
});

export function voucherLinksFromJSON(
  jsonString: string,
): SafeParseResult<VoucherLinks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VoucherLinks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VoucherLinks' from JSON`,
  );
}

/** @internal */
export const Voucher$inboundSchema: z.ZodType<Voucher, z.ZodTypeDef, unknown> =
  z.object({
    resource: z.string(),
    id: z.string(),
    name: z.string(),
    image: z.lazy(() => VoucherImage$inboundSchema),
    status: VoucherStatus$inboundSchema,
    contractor: z.lazy(() => Contractor$inboundSchema),
    _links: z.lazy(() => VoucherLinks$inboundSchema),
  }).transform((v) => {
    return remap$(v, {
      "_links": "links",
    });
  });

export function voucherFromJSON(
  jsonString: string,
): SafeParseResult<Voucher, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Voucher$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Voucher' from JSON`,
  );
}
