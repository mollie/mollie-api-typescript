/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
 */
export type AmountNullable = {
  /**
   * A three-character ISO 4217 currency code.
   */
  currency: string;
  /**
   * A string containing an exact monetary amount in the given currency.
   */
  value: string;
};

/** @internal */
export const AmountNullable$inboundSchema: z.ZodType<
  AmountNullable,
  z.ZodTypeDef,
  unknown
> = z.object({
  currency: z.string(),
  value: z.string(),
});
/** @internal */
export type AmountNullable$Outbound = {
  currency: string;
  value: string;
};

/** @internal */
export const AmountNullable$outboundSchema: z.ZodType<
  AmountNullable$Outbound,
  z.ZodTypeDef,
  AmountNullable
> = z.object({
  currency: z.string(),
  value: z.string(),
});

export function amountNullableToJSON(amountNullable: AmountNullable): string {
  return JSON.stringify(AmountNullable$outboundSchema.parse(amountNullable));
}
export function amountNullableFromJSON(
  jsonString: string,
): SafeParseResult<AmountNullable, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AmountNullable$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AmountNullable' from JSON`,
  );
}
