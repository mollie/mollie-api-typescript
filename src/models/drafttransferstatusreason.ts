/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d227620000de
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  DraftTransferStatusReasonCodeResponse,
  DraftTransferStatusReasonCodeResponse$inboundSchema,
} from "./drafttransferstatusreasoncoderesponse.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The reason for the draft transfer's current status. `null` unless `status` is `declined`.
 */
export type DraftTransferStatusReason = {
  /**
   * A machine-readable code that indicates the reason for the draft transfer's current status.
   */
  code: DraftTransferStatusReasonCodeResponse;
  /**
   * A human-readable explanation of the status reason. For `declined-by-initiator`, this includes the
   *
   * @remarks
   * initiator's free-text reason when they provided one.
   */
  message: string;
};

/** @internal */
export const DraftTransferStatusReason$inboundSchema: z.ZodType<
  DraftTransferStatusReason,
  z.ZodTypeDef,
  unknown
> = z.object({
  code: DraftTransferStatusReasonCodeResponse$inboundSchema,
  message: z.string(),
});

export function draftTransferStatusReasonFromJSON(
  jsonString: string,
): SafeParseResult<DraftTransferStatusReason, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DraftTransferStatusReason$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DraftTransferStatusReason' from JSON`,
  );
}
