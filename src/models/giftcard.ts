/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f49902771954
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Url, Url$inboundSchema } from "./url.js";

/**
 * The status of the issuer.
 *
 * @remarks
 * If the status is `pending-issuer`, an additional action from your side may be required with the issuer.
 */
export const GiftcardStatus = {
  Activated: "activated",
  PendingIssuer: "pending-issuer",
} as const;
/**
 * The status of the issuer.
 *
 * @remarks
 * If the status is `pending-issuer`, an additional action from your side may be required with the issuer.
 */
export type GiftcardStatus = OpenEnum<typeof GiftcardStatus>;

/**
 * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
 */
export type GiftcardLinks = {
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  self?: Url | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  documentation?: Url | undefined;
};

export type Giftcard = {
  /**
   * Indicates the response contains a payment method issuer object. Will always contain the string `issuer` for this
   *
   * @remarks
   * endpoint.
   */
  resource: string;
  /**
   * The unique identifier of the payment method issuer.
   */
  id: string;
  /**
   * The full name of the payment method issuer.
   */
  description: string;
  status: GiftcardStatus;
  /**
   * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
   */
  links: GiftcardLinks;
};

/** @internal */
export const GiftcardStatus$inboundSchema: z.ZodType<
  GiftcardStatus,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(GiftcardStatus);

/** @internal */
export const GiftcardLinks$inboundSchema: z.ZodType<
  GiftcardLinks,
  z.ZodTypeDef,
  unknown
> = z.object({
  self: Url$inboundSchema.optional(),
  documentation: Url$inboundSchema.optional(),
});

export function giftcardLinksFromJSON(
  jsonString: string,
): SafeParseResult<GiftcardLinks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GiftcardLinks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GiftcardLinks' from JSON`,
  );
}

/** @internal */
export const Giftcard$inboundSchema: z.ZodType<
  Giftcard,
  z.ZodTypeDef,
  unknown
> = z.object({
  resource: z.string(),
  id: z.string(),
  description: z.string(),
  status: GiftcardStatus$inboundSchema,
  _links: z.lazy(() => GiftcardLinks$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
  });
});

export function giftcardFromJSON(
  jsonString: string,
): SafeParseResult<Giftcard, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Giftcard$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Giftcard' from JSON`,
  );
}
