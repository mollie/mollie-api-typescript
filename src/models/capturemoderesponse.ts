/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  catchUnrecognizedEnum,
  OpenEnum,
  Unrecognized,
} from "../types/enums.js";

/**
 * Indicate if the funds should be captured immediately or if you want to [place a hold](https://docs.mollie.com/docs/place-a-hold-for-a-payment#/)
 *
 * @remarks
 * and capture at a later time.
 *
 * This field needs to be set to `manual` for method `riverty`.
 */
export const CaptureModeResponse = {
  Automatic: "automatic",
  Manual: "manual",
} as const;
/**
 * Indicate if the funds should be captured immediately or if you want to [place a hold](https://docs.mollie.com/docs/place-a-hold-for-a-payment#/)
 *
 * @remarks
 * and capture at a later time.
 *
 * This field needs to be set to `manual` for method `riverty`.
 */
export type CaptureModeResponse = OpenEnum<typeof CaptureModeResponse>;

/** @internal */
export const CaptureModeResponse$inboundSchema: z.ZodType<
  CaptureModeResponse,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(CaptureModeResponse),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const CaptureModeResponse$outboundSchema: z.ZodType<
  CaptureModeResponse,
  z.ZodTypeDef,
  CaptureModeResponse
> = z.union([
  z.nativeEnum(CaptureModeResponse),
  z.string().and(z.custom<Unrecognized<string>>()),
]);
