/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f064252d925b
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { Amount, Amount$inboundSchema } from "./amount.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Metadata, Metadata$inboundSchema } from "./metadata.js";
import { Mode, Mode$inboundSchema } from "./mode.js";
import {
  RefundExternalReferenceTypeResponse,
  RefundExternalReferenceTypeResponse$inboundSchema,
} from "./refundexternalreferencetyperesponse.js";
import { Url, Url$inboundSchema } from "./url.js";
import { UrlNullable, UrlNullable$inboundSchema } from "./urlnullable.js";

export const RefundResponseStatus = {
  Queued: "queued",
  Pending: "pending",
  Processing: "processing",
  Refunded: "refunded",
  Failed: "failed",
  Canceled: "canceled",
} as const;
export type RefundResponseStatus = OpenEnum<typeof RefundResponseStatus>;

export type RefundResponseExternalReference = {
  /**
   * Specifies the reference type
   */
  type?: RefundExternalReferenceTypeResponse | undefined;
  /**
   * Unique reference from the payment provider
   */
  id?: string | undefined;
};

/**
 * Where the funds will be pulled back from.
 */
export type RefundResponseSource = {
  organizationId?: string | undefined;
};

export type RefundResponseRoutingReversal = {
  /**
   * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
   */
  amount?: Amount | undefined;
  /**
   * Where the funds will be pulled back from.
   */
  source?: RefundResponseSource | undefined;
};

/**
 * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
 */
export type RefundResponseLinks = {
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  self: Url;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  payment: Url;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  settlement?: UrlNullable | null | undefined;
  /**
   * In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field.
   */
  documentation: Url;
};

export type RefundResponse = {
  /**
   * Indicates the response contains a refund object. Will always contain the string `refund` for this endpoint.
   */
  resource: string;
  /**
   * The identifier uniquely referring to this refund. Mollie assigns this identifier at refund creation time. Mollie
   *
   * @remarks
   * will always refer to the refund by this ID. Example: `re_4qqhO89gsT`.
   */
  id: string;
  /**
   * Whether this entity was created in live mode or in test mode.
   */
  mode: Mode;
  /**
   * The description of the refund that may be shown to your customer, depending on the payment method used.
   */
  description: string;
  /**
   * In v2 endpoints, monetary amounts are represented as objects with a `currency` and `value` field.
   */
  amount: Amount;
  /**
   * Provide any data you like, for example a string or a JSON object. We will save the data alongside the entity. Whenever
   *
   * @remarks
   * you fetch the entity with our API, we will also include the metadata. You can use up to approximately 1kB.
   */
  metadata: Metadata | null;
  /**
   * The unique identifier of the payment this refund was created for.
   *
   * @remarks
   * The full payment object can be retrieved via the payment URL in the `_links` object.
   */
  paymentId: string;
  /**
   * The identifier referring to the settlement this refund was settled with. This field is omitted if the refund is not settled (yet).
   */
  settlementId?: string | null | undefined;
  status: RefundResponseStatus;
  /**
   * The entity's date and time of creation, in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
   */
  createdAt: string;
  externalReference?: RefundResponseExternalReference | undefined;
  /**
   * *This feature is only available to marketplace operators.*
   *
   * @remarks
   *
   * When creating refunds for *routed* payments, by default the full amount is deducted from your balance.
   *
   * If you want to pull back funds from the connected merchant(s), you can use this parameter to specify what amount
   * needs to be reversed from which merchant(s).
   *
   * If you simply want to fully reverse the routed funds, you can also use the `reverseRouting` parameter instead.
   */
  routingReversals?: Array<RefundResponseRoutingReversal> | null | undefined;
  /**
   * An object with several relevant URLs. Every URL object will contain an `href` and a `type` field.
   */
  links: RefundResponseLinks;
};

/** @internal */
export const RefundResponseStatus$inboundSchema: z.ZodType<
  RefundResponseStatus,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(RefundResponseStatus);

/** @internal */
export const RefundResponseExternalReference$inboundSchema: z.ZodType<
  RefundResponseExternalReference,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: RefundExternalReferenceTypeResponse$inboundSchema.optional(),
  id: z.string().optional(),
});

export function refundResponseExternalReferenceFromJSON(
  jsonString: string,
): SafeParseResult<RefundResponseExternalReference, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefundResponseExternalReference$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefundResponseExternalReference' from JSON`,
  );
}

/** @internal */
export const RefundResponseSource$inboundSchema: z.ZodType<
  RefundResponseSource,
  z.ZodTypeDef,
  unknown
> = z.object({
  organizationId: z.string().optional(),
});

export function refundResponseSourceFromJSON(
  jsonString: string,
): SafeParseResult<RefundResponseSource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefundResponseSource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefundResponseSource' from JSON`,
  );
}

/** @internal */
export const RefundResponseRoutingReversal$inboundSchema: z.ZodType<
  RefundResponseRoutingReversal,
  z.ZodTypeDef,
  unknown
> = z.object({
  amount: Amount$inboundSchema.optional(),
  source: z.lazy(() => RefundResponseSource$inboundSchema).optional(),
});

export function refundResponseRoutingReversalFromJSON(
  jsonString: string,
): SafeParseResult<RefundResponseRoutingReversal, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefundResponseRoutingReversal$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefundResponseRoutingReversal' from JSON`,
  );
}

/** @internal */
export const RefundResponseLinks$inboundSchema: z.ZodType<
  RefundResponseLinks,
  z.ZodTypeDef,
  unknown
> = z.object({
  self: Url$inboundSchema,
  payment: Url$inboundSchema,
  settlement: z.nullable(UrlNullable$inboundSchema).optional(),
  documentation: Url$inboundSchema,
});

export function refundResponseLinksFromJSON(
  jsonString: string,
): SafeParseResult<RefundResponseLinks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefundResponseLinks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefundResponseLinks' from JSON`,
  );
}

/** @internal */
export const RefundResponse$inboundSchema: z.ZodType<
  RefundResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  resource: z.string(),
  id: z.string(),
  mode: Mode$inboundSchema,
  description: z.string(),
  amount: Amount$inboundSchema,
  metadata: z.nullable(Metadata$inboundSchema),
  paymentId: z.string(),
  settlementId: z.nullable(z.string()).optional(),
  status: RefundResponseStatus$inboundSchema,
  createdAt: z.string(),
  externalReference: z.lazy(() => RefundResponseExternalReference$inboundSchema)
    .optional(),
  routingReversals: z.nullable(
    z.array(z.lazy(() => RefundResponseRoutingReversal$inboundSchema)),
  ).optional(),
  _links: z.lazy(() => RefundResponseLinks$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
  });
});

export function refundResponseFromJSON(
  jsonString: string,
): SafeParseResult<RefundResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefundResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefundResponse' from JSON`,
  );
}
